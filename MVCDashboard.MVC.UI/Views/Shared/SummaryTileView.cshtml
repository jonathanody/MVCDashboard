@using MVCDashboard.UI.MVC.Models
@model SummaryTileViewModel

<div class="slide image" >
	<div class="tile double noHover @Model.BackgroundCSSClass">
		<div class="tile-content">
			<h3>@Model.Name</h3>			
		</div>
		<div class="brand summaryTileBrand">
			@*<a href="@Url.Action("Index", "Misc")">@Model.Count</a>*@
			<span class="summaryTileCount">@Model.Count</span>
		</div>
	</div>
</div>